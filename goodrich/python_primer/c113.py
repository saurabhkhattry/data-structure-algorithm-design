"""
C 1.13
---------------------------------
Problem Statement : Write a pseudo-code description of a function that reverses a list of n
integers, so that the numbers are listed in the opposite order than they
were before, and compare this method to an equivalent Python function
for doing the same thing.
Author : Saurabh
"""

"""
def reverse_func(l):
    r = []
    length_l = len(l)
    for x in range(length_l):
        r.append(l[0-x])
    return r
"""
